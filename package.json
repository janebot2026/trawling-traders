{
  "name": "trawling-traders",
  "version": "0.1.0",
  "private": true,
  "description": "React Native app for managing OpenClaw trading bots",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:mobile": "cd apps/mobile && npm run start",
    "build:mobile": "cd apps/mobile && npm run build",
    "lint": "npm --workspace @trawling-traders/types run build && npm --workspace @trawling-traders/api-client run build",
    "typecheck": "npm --workspace @trawling-traders/types run build && npm --workspace @trawling-traders/api-client run build",
    "postinstall": "npx patch-package"
  },
  "devDependencies": {
    "@types/react": "18.2.79",
    "@types/node": "^20",
    "typescript": "^5.3"
  },
  "overrides": {
    "@types/react": "18.2.79"
  }
}
