# Requires a .env file (see .env.example for template).
# DEVELOPMENT ONLY - These credentials are NOT for production use.
# In production, use proper secrets management (e.g., Kubernetes secrets, Vault).
services:
  postgres:
    image: postgres:16-alpine
    container_name: trawling-traders-db
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER}"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
